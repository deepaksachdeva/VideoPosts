<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item
        android:id="@+id/action_sorted_by_date"
        android:orderInCategory="300"
        android:title="@string/by_date"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/action_sorted_by_likes"
        android:orderInCategory="300"
        android:title="@string/by_likes"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/action_sorted_by_views"
        android:orderInCategory="300"
        android:title="@string/by_views"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/action_sorted_by_shares"
        android:orderInCategory="300"
        android:title="@string/by_shares"
        app:showAsAction="never">
    </item>
</menu>